# Classroom Occupancy Detector — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core Computer Vision ─────────────────────────────────
ultralytics>=8.0.0          # YOLOv8 + ByteTrack tracker
opencv-python>=4.8.0        # Camera capture, frame processing, annotation

# ── Numerical / Data ─────────────────────────────────────
numpy>=1.24.0
pandas>=1.5.0

# ── Web Dashboard ─────────────────────────────────────────
streamlit>=1.28.0

# ── Database ──────────────────────────────────────────────
# sqlite3 is part of Python standard library — no extra install needed

# ── Optional: GPU acceleration (comment out if no CUDA) ──
# torch>=2.0.0+cu118          # Install manually from pytorch.org for GPU

